/*
wget https://packages.microsoft.com/config/debian/10/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
sudo apt-get update
sudo apt-get install -y apt-transport-https
sudo apt-get update
sudo apt-get install -y dotnet-sdk-7.0
*/

// dotnet new console
// dotnet build 
// dotnet run

/* 
gitpod /workspace/programminghub (main) $ cd /workspace/programminghub/c#
gitpod /workspace/programminghub/c# (main) $ dotnet build 
*/

/* 
We use Integer type of variables to store whole numbers – keyword “int”
For storing the Decimal type of numbers, we use decimal variables – keyword “double”
If we want to store a single character, we use character variables – keyword “char”
In the case of storing words or sentences. we use the String variables - keyword “string”
*/
/*
sudo apt-cache search dotnet-sdk search for the SDK
*/